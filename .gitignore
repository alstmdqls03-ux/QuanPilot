# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# 환경 변수 / 시크릿 (절대 커밋 금지)
.env

# 로컬 데이터 (SQLite DB, 수집한 시장 데이터)
# WHY: 루트의 data/ 디렉토리만 무시하되, 소스 패키지 quantpilot/data/ 는 제외.
/data/
*.db
!quantpilot/data/

# 백테스트 산출물 (equity curve PNG 등 런타임 결과)
# WHY: backtest 명령이 루트에 생성하는 차트는 재생성 가능한 산출물이라 추적 안 함.
backtest_*.png

# OS / 에디터
.DS_Store
.idea/
.vscode/

# git worktree 작업 폴더 (격리 작업공간 — 레포에 딸려가면 안 됨)
.worktrees/

# Claude Code / GSD 로컬 툴링 (네이티브 worktree·세션·플래닝 산출물)
# WHY: .claude/worktrees는 격리 체크아웃(venv·DB 포함), .planning은 로컬 플래닝 상태.
#   둘 다 레포에 안 올린다. CLAUDE.md(루트)는 추적 대상이라 영향 없음.
.claude/
.planning/
